[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fact-forensic-toolkit"
version = "2.13.0"
description = "Source-agnostic forensic acquisition, capture, packaging, signing, and verification toolkit"
readme = "README.md"
requires-python = ">=3.11"
license = "LGPL-2.1-or-later"
authors = [{name = "FACT contributors"}]
dependencies = []

[project.optional-dependencies]
screenshot = [
    "dbus-next>=0.2.3,<1",
]
dev = [
    "build>=1.2",
    "pytest>=8",
    "pytest-cov>=6",
    "ruff==0.16.6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "--strict-config",
    "--strict-markers",
    "--cov=fact",
    "--cov-report=term-missing",
    "--cov-report=xml",
]

[tool.coverage.run]
branch = true
source = ["fact"]
omit = [
    "src/fact/__main__.py",
]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 80

[tool.ruff]
target-version = "py311"
line-length = 88
src = ["src", "tests"]

[tool.ruff.lint]
ignore = ["E501"]
select = [
    "E",    # pycodestyle errors
    "F",    # Pyflakes, including unused imports
    "I",    # import ordering
    "B",    # flake8-bugbear
    "UP",   # Python syntax modernisation
    "SIM",  # simplification suggestions
    "RUF",  # Ruff-specific checks
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
    "S101",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
docstring-code-format = true

[project.scripts]
fact = "fact.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
